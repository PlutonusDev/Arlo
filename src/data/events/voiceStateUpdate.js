module.exports = (azure, oldState, newState) => {
    /*return console.log(newState);
    if(oldState.member.user === azure.client.user && !newState.connection) {
        if(azure.musicQueue.get(oldState.guild.id)) {
            azure.musicQueue.get(oldState.guild.id).textChannel.send({embed:{
                author: {
                    name: "Disconnect",
                    icon_url: ""
                },
                description: "I have disconnected from my voice channel."
            }})
            azure.musicQueue.delete(oldState.guild.id);
        }
    }*/
}